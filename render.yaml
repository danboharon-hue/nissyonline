services:
  - type: web
    name: nissyonline
    runtime: node
    plan: free
    buildCommand: bash build.sh
    startCommand: node server.js
    envVars:
      - key: NISSY_PATH
        value: /opt/render/project/src/nissy
      - key: PORT
        value: 10000
